sudo yum update -y
sudo yum install -y docker
sudo service docker start
sudo docker pull mfeurer/auto-sklearn
sudo docker ps -a
docker run  -it -p 8080:8080  -it mfeurer/auto-sklearn
> (in docker) jupyter notebook --ip=0.0.0.0 --port=8080 --allow-root
